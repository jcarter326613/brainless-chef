{
  "name": "brainless-chef",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "dev": "pnpm --parallel --stream --filter @brainless-chef/api --filter @brainless-chef/web dev",
    "dev:api": "pnpm --filter @brainless-chef/api dev",
    "dev:web": "pnpm --filter @brainless-chef/web dev",
    "test": "pnpm -r test",
    "test:integration": "firebase emulators:exec --only firestore --project brainless-chef-integration \"GCLOUD_PROJECT=brainless-chef-integration pnpm --filter @brainless-chef/firestore-database test:integration\"",
    "typecheck": "pnpm -r --filter './packages/*' build && pnpm -r typecheck"
  },
  "devDependencies": {
    "firebase-tools": "^14.27.0"
  }
}
